// create an array variable containing the names
let names = ["Amy", "Leslie", "Chris", "Andy", "Jen", "Collin", "Jan", "Adam", "Wyatt", "Margo"];
// write a for loop that prints each name on a different line
for (let i = 0; i < names.length; i++)
    {
        console.log(names[i]);
    }